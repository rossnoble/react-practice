# Sortable List

Build a drag-and-drop sortable list using native HTML5 drag events.

## Requirements
- Reorder items by dragging
- Visual feedback during drag (e.g. placeholder, opacity change)
- Drop indicator showing where item will be placed
- Update list state on drop
- No external libraries
